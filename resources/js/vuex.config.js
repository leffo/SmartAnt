import Vue from "vue";
import Vuex from "vuex";
import apollo from "./apollo.config";

Vue.use(Vuex);

const store = {
    state: {},
    mutations: {},
    actions: {}
};

export default new Vuex.Store(store);
